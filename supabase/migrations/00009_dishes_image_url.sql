-- Колонка с фотографией блюда.
-- Форма публикации блюда (app/admin/dashboard + lib/menu/dishes.ts) сохраняет фото
-- как сжатый Canvas-JPEG в виде data URL. Без этой колонки INSERT блюда с фотографией
-- отклонялся целиком, и блюдо оставалось только в localStorage того браузера, где его
-- опубликовали (на других устройствах не появлялось).
--
-- Применена вручную на Production Supabase 28.08.2026. Требует ручного применения на
-- любом новом окружении (по аналогии с миграциями 00006–00008).

ALTER TABLE public.dishes
  ADD COLUMN IF NOT EXISTS image_url TEXT;
