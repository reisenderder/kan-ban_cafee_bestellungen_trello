-- Добавление мягкого удаления (архивирования) блюд меню.
-- Архивированное блюдо скрывается из панели Администратора и с витрины,
-- но карточка блюда не удаляется физически (соответствует политике проекта
-- "удаление важных рабочих записей заменяется архивированием").

ALTER TABLE public.dishes
  ADD COLUMN IF NOT EXISTS is_archived BOOLEAN NOT NULL DEFAULT false;
