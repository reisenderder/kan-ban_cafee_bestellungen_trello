# Technical Spec: Доступы, маскирование данных и аудит (Supabase Auth & RLS)

> **Статус**: Проверено и зафиксировано (Группа 4: Данные, Auth и безопасность Supabase)
> **Дата создания**: 2026-07-21
> **Дата обновления**: 2026-08-29
> **Версия**: 1.3

> **[Правка Блока 2 — 2026-08-29]** Правки под пункты 4, 5, 8 плана
> `../../work_plans/active/Work_Plan_Block_2_Ops_Auth_Polish.md`: роль `KITCHEN` и
> фазность выдачи учётных записей (§2); строка «Навигация между служебными дэшбордами»
> в матрице доступа (§4); межконтурная навигация менеджера (§9) и администратора (§11);
> начальная выдача `ADMIN`/`MANAGER`, непубличная точка входа персонала, восстановление
> RLS и код доступа клиента к заказу (§18 пп. 8–11).
> **Источник**: `../01_global_spec/Global_Spec.md`, `../02_functional_map/Functional_Map.md`, `../03_feature_specs/Feature_Admin_Control.md`, `../03_feature_specs/Feature_Order_CRM.md`, `../03_feature_specs/Feature_Courier_Delivery.md`, `../03_feature_specs/Feature_Resolution_Department.md`, `../03_feature_specs/Feature_Complaints.md`, `../03_feature_specs/Feature_Client_Chat.md`, `../03_feature_specs/Feature_Route_Batching.md`, `Technical_MVP_Implementation_Decisions.md`

---

## 1. Назначение

Этот документ описывает технические правила доступа к клиентским данным, аутентификацию сотрудников через **Supabase Auth**, разграничение прав на уровне СУБД с помощью **Supabase RLS (Row Level Security)**, маскирование, аудит раскрытия данных и правила совпадения признаков для желтого и красного списка в рамках Группы 4 плана `../../work_plans/active/Work_Plan_Code_Readiness.md`.

Главный принцип: сотрудник видит только те данные, которые нужны его роли и конкретной ситуации. Доступ к клиентским данным контекстен, строго ограничен и журналируется.

---

## 2. Роли первого уровня и Supabase Auth

На первом этапе система использует четыре роли сотрудников:

1. `Администратор` (`ADMIN`)
2. `Менеджер` (`MANAGER`)
3. `Курьер` (`COURIER`)
4. `Отдел урегулирования` (`RESOLUTION_OFFICER`)

`Клиент` и `Система` являются типами actor/principal для правил доступа и аудита, но **не имеют учетных записей Supabase Auth**.

`Owner` — бизнес-персона владельца кафе, использующая роль `Администратор`. Отдельная техническая роль `Owner` в MVP не создается.

### Кухня как актор в MVP

> **Решение для MVP:** Кухня работает по концепции «бумажная кухня» и не имеет собственной учётной записи в системе. Все действия, связанные с кухней (передача заказа, отметка готовности), выполняются менеджером под ролью `MANAGER`. Если в будущем появится экран на кухне — будет добавлена отдельная роль `KITCHEN` с минимальными правами (только просмотр назначенных чеков и отметка готовности).

> **[Правка Блока 2 — 2026-08-29]:** Электронный экран кухни (KDS) `/kitchen/dashboard`
> уже используется, поэтому роль `KITCHEN` вводится сейчас с минимальными правами:
> просмотр назначенных кухонных чеков и отметка готовности «ГОТОВО». Без доступа к
> телефону, адресу, геолокации, Telegram/Email, оплате, клиентскому чату и без
> навигации в другие контуры. Бумажная кухня остаётся допустимым режимом параллельно.

### Фазность выдачи учётных записей (Блок 2)

Модель ролей полная, но реальные учётные записи Supabase Auth создаются поэтапно:

* **Блок 2:** заводятся только `ADMIN` и `MANAGER`.
* `COURIER`, `RESOLUTION_OFFICER`, `KITCHEN` — определены в модели и в RLS, но без
  учётных записей до соответствующих будущих блоков.

Ролевая навигация (§4) описывает целевое состояние для всех ролей; фактически в
Блоке 2 работает вход только для `ADMIN` и `MANAGER`.

### Аутентификация и сессии сотрудников:

1. Учетные записи сотрудников создаются в **Supabase Auth** администратором.
2. Роль сотрудника передается в JWT-токене через `app_metadata` (`auth.jwt() -> 'app_metadata' -> 'role'`).
3. При изменении статуса сотрудника на `Отключен` (`DISABLED`) или `Архив` (`ARCHIVED`):
   * На сервере вызывается `supabase.auth.admin.signOut(userId)` для моментального аннулирования активных refresh-токенов;
   * Таблица профилей сотрудников `employee_profiles.status` переходит в `DISABLED` / `ARCHIVED`;
   * Политики Supabase RLS при каждом запросе выполняют проверку `employee_profiles.status = 'ACTIVE'`, блокируя любой доступ СУБД для отключенного сотрудника вне зависимости от наличия старого токена.

---

## 3. Базовая модель доступа

Доступ должен строиться по трем условиям:

1. **Роль** - что сотрудник вообще может делать.
2. **Контекст** - к какому заказу, жалобе, ситуации или товару относится действие.
3. **Состояние** - находится ли заказ, доставка, жалоба или ситуация в статусе, где доступ оправдан.

Если нет роли, контекста или состояния, доступ должен быть закрыт.

Пример:

* курьер может видеть телефон клиента только по назначенному ему заказу;
* после закрытия доставки курьер теряет доступ к телефону, адресу, ориентиру и геолокации;
* если курьеру снова нужны данные, он запрашивает их через менеджера.

---

## 4. Матрица доступа

| Данные / действие | Администратор | Менеджер | Курьер | Отдел урегулирования |
| --- | --- | --- | --- | --- |
| Просмотр текущих заказов | Да | Да | Только назначенные | Только переданные в урегулирование |
| Телефон клиента | Да, структурно | Да, по заказу | Только назначенная доставка | Да, по ситуации |
| Адрес и ориентир | Да, структурно | Да, по заказу | Только назначенная доставка | Да, по ситуации |
| Геолокация | Да, структурно | Да, по заказу | Только назначенная доставка | Да, по ситуации |
| Telegram / Email | Да, структурно | Ограниченно по заказу | Нет | Да, по ситуации |
| Пол / формат обращения | Не как обычное поле заказа | Нет | Нет | Да, для маршрутизации |
| Клиентский чат | Да, структурно | Да, по заказу | Нет | Только если передано в ситуацию |
| Жалобы | Да | Нет как управление | Нет | Только если администратор использует отдел как ресурс |
| Желтый список | Да, просмотр | Предупреждение по заказу | Нет | Полное управление |
| Красный список | Да, просмотр | Предупреждение по заказу | Нет | Полное управление |
| Управление меню | Да | Только доступность блюд | Нет | Нет |
| Скидочные товары | Да | Только при выданном праве | Нет | Нет |
| Закрытие жалоб | Да | Нет | Нет | Нет |
| Снятие желтого/красного списка | Нет | Нет | Нет | Да |
| Навигация между служебными дэшбордами *(Блок 2)* | Все, включая экран администратора | Все, кроме экрана администратора | Только свой экран | Все, кроме экрана администратора |

Строка «Кухня» (`KITCHEN`) в этой матрице: доступ только к экрану кухни (KDS), навигации в другие контуры нет.

Администратор видит данные через структурированные разделы и журналы, но это не означает свободный неограниченный экспорт клиентской базы.

> **[Правка Блока 2 — 2026-08-29] Навигация ≠ раскрытие данных.** Строка «Навигация между
> служебными дэшбордами» даёт роли возможность **открыть экран** другого контура. Она не
> расширяет права на клиентские данные: на каждом экране продолжают действовать правила
> маскирования (§5), контекст (§3) и раскрытие по действию с журналированием (§6). То
> есть менеджер может открыть кабинет курьера или экран урегулирования, но видит там
> данные по тем же правилам роль × контекст × состояние. Экран администратора остаётся
> доступен только роли `ADMIN`.

---

## 5. Маскирование данных

Маскирование нужно для обычного рабочего режима, где сотруднику не требуется видеть полные данные.

Минимальные правила:

* телефон показывается полностью только там, где нужен звонок или контакт;
* Telegram и Email не показываются курьеру;
* пол клиента не показывается менеджеру и курьеру;
* желтый и красный список не показываются курьеру;
* внутренние причины риска не показываются клиенту;
* платежные детали не показываются кухне и курьеру сверх нужного сообщения;
* кухня не видит телефон, адрес, геолокацию, Telegram, Email и оплату.

Примеры маскирования:

* телефон в списке заказов менеджера: `+20 *** *** 1234`, если полный номер не нужен прямо сейчас;
* email в журнале администратора: `a***@domain.com`, если полный email не нужен для действия;
* Telegram: показывать как подтвержденный канал без раскрытия лишних деталей, если не требуется контакт.

Точные форматы маскирования могут быть уточнены в UI/User Stories, но принцип должен сохраняться: полные данные раскрываются только при необходимости.

---

## 6. Раскрытие данных

Раскрытие данных - это действие, при котором сотрудник получает полный контакт, адрес, геолокацию или другой чувствительный элемент, который обычно скрыт.

Раскрытие должно быть:

* привязано к роли;
* привязано к заказу, доставке, жалобе или ситуации;
* ограничено временем и состоянием;
* записано в журнал.

Минимальные события раскрытия:

* менеджер открывает полный телефон клиента;
* менеджер открывает полную геолокацию;
* курьер получает данные назначенной доставки;
* курьер теряет данные после закрытия доставки;
* отдел урегулирования открывает Telegram/Email/телефон клиента;
* администратор открывает структурированные клиентские данные;
* менеджер передает ситуацию в урегулирование;
* сотрудник запрашивает повторное раскрытие данных после закрытия доставки.

---

## 7. Аудит

Система должна вести журнал действий по доступу и важным операциям.

Минимальные поля журнала:

* дата и время;
* сотрудник или системный actor;
* роль;
* действие;
* объект действия;
* причина или контекст;
* результат;
* старое значение и новое значение, если было изменение;
* источник действия: интерфейс, системное событие, автоматический процесс.

Журнал должен фиксировать:

* вход сотрудника в систему;
* создание, изменение и отключение сотрудника;
* назначение роли;
* раскрытие клиентских данных;
* назначение курьера;
* передачу заказа на кухню;
* подтверждение оплаты;
* QR/fallback подтверждение доставки;
* передачу в урегулирование;
* действия с желтым и красным списком;
* действия с жалобами;
* экстренное изменение цены администратором в рабочее время: обязательная причина, просмотр влияния на активные корзины, повторное подтверждение и результат;
* выдачу менеджеру права на скидочный товар.

---

## 8. Курьерский доступ

Курьер получает данные только по назначенным доставкам.

Курьер может видеть:

* номер назначенного заказа;
* адрес;
* ориентир;
* геолокацию;
* телефон для звонка;
* состав заказа, если это нужно для доставки;
* итоговую сумму только при наличной оплате;
* сообщение `Оплату брать не нужно`, если электронная оплата подтверждена;
* инструкцию менеджера, если она есть.

Курьер не может видеть:

* клиентскую базу;
* чужие заказы;
* Telegram;
* Email;
* пол клиента;
* внутренние причины желтого/красного списка;
* историю клиента;
* жалобы;
* платежные детали;
* данные после закрытия доставки.

После закрытия доставки система должна закрыть доступ курьера к телефону, адресу, ориентиру и геолокации по этому заказу.

---

## 9. Менеджерский доступ

Менеджер работает с операционными заказами.

Менеджер видит:

* подтвержденные рабочие заказы;
* данные, нужные для обработки заказа;
* адрес, ориентир и геолокацию;
* телефон для связи и доставки;
* клиентский чат по заказу;
* способ оплаты и состояние оплаты;
* предупреждение о риске, если заказ похож на желтый/красный случай;
* вкладки `На доставке` и `На урегулировании`;
* счетчики непрочитанных сообщений.

Менеджер не видит:

* неподтвержденные попытки как обычные заказы;
* пол клиента как обычное поле заказа;
* полную внутреннюю логику желтого/красного списка;
* жалобы как управляемые задачи;
* клиентскую базу вне рабочего контекста;
* экран администратора.

**(Блок 2)** Менеджер имеет межконтурную навигацию из своей CRM: экран кухни (KDS),
кабинет курьера, экран отдела урегулирования. На каждом из этих экранов данные видны по
правилам роль × контекст × состояние и маскирования (§5). Служебная полоска навигации
показывается по роли вошедшего, а не по странице: менеджер, открывший экран кухни,
всегда может вернуться в CRM в один клик.

Если менеджер передает ситуацию в отдел урегулирования, система сама маршрутизирует ситуацию в нужное направление. Менеджер не выбирает мужской или женский отдел вручную.

---

## 10. Доступ отдела урегулирования

Отдел урегулирования получает доступ только в рамках конкретной ситуации.

Отдел урегулирования видит:

* неподтвержденные попытки;
* ситуации, переданные менеджером;
* ситуации, возникшие при недоступном клиенте;
* Telegram/Email/телефон/адрес/геолокацию по ситуации;
* дополнительные контакты, если клиент указал их добровольно;
* признаки желтого/красного списка;
* историю связанных проблем.

Отдел урегулирования может:

* связаться с клиентом;
* вернуть ситуацию менеджеру;
* архивировать неподтвержденную попытку;
* снять запись с желтого списка;
* снять запись с красного списка;
* зафиксировать результат контакта;
* принять финальное решение о возврате заказа.

---

## 11. Администраторский доступ

Администратор имеет широкие права внутри платформы, но не имеет права заходить в техническую часть, менять код или выполнять действия вне продуктовой платформы.

Администратор может:

* управлять сотрудниками;
* назначать роли;
* отключать и восстанавливать доступ;
* управлять меню и ценами;
* управлять скидочными товарами;
* выдавать менеджеру право на конкретный скидочный товар;
* видеть жалобы;
* закрывать, архивировать, перемещать и логически скрывать жалобы; действие `Удалить` является soft delete без физического удаления карточки и журнала;
* видеть структурированные клиентские данные;
* видеть журналы действий;
* **(Блок 2)** навигационно открывать любой служебный дэшборд из своей панели (CRM, KDS, кабинет курьера, экран урегулирования); неготовые разделы помечены «(на разработке)». Правила маскирования данных на этих экранах сохраняются. Экран администратора доступен только роли `ADMIN`.

Администратор не должен:

* снимать желтый или красный список вместо отдела урегулирования;
* превращать ограниченные роли в свободный доступ к клиентской базе;
* обходить утвержденный процесс жалоб;
* менять технический код системы из платформы.

> **Примечание по расширению (Post-MVP)**: Уведомления о входах/выходах сотрудников и новых заказах в личный Telegram/группу с темами зафиксированы в архитектуре, но отложены до следующего релиза, чтобы не задерживать первый боевой запуск Блока 1.

---

## 12. Желтый и красный список

Желтый и красный список строятся не по одному признаку, а по набору совпадений.

Базовые признаки для сравнения:

* геолокация;
* адрес;
* телефон;
* Telegram;
* Email;
* клиентский профиль;
* устройство;
* имя;
* история заказов.

Дополнительный контакт не является десятой категорией: телефон, Telegram или Email из дополнительного контакта нормализуется и сравнивается внутри соответствующей существующей категории. Один и тот же факт нельзя посчитать дважды.

Порог желтого риска в MVP равен 4: при совпадении 4 или более нормализованных категорий признаков система создает желтую risk-запись и предупреждение. При 3 или меньшем числе совпавших категорий желтая запись не создается.

Красный список возникает после одного подтвержденного повторения проблемного случая, уже связанного с активной желтой записью, в рамках утвержденных причин:

* клиент недоступен;
* конфликт;
* ложный заказ;
* отказ от оплаты.

---

## 13. Правило совпадения признаков

Технически совпадение признаков должно считаться не как точное равенство одной строки, а как набор нормализованных сравнений.

Примерная логика:

1. Система собирает признаки нового заказа.
2. Система нормализует каждую категорию признаков: геолокацию, адрес, телефон, Telegram, Email, клиентский профиль, устройство, имя и связанную историю заказов.
3. Система сравнивает новый заказ с активными риск-записями.
4. Система учитывает не более одного совпадения на категорию и считает число совпавших категорий; веса в MVP не используются.
5. При результате `>= 4` система создает желтую risk-запись и показывает менеджеру предупреждение.
6. Новый проблемный случай, связанный с активной желтой записью, передается в отдел урегулирования для подтверждения повторения.
7. После одного подтвержденного повторения система переводит risk-запись в красную.

Система не должна переводить подтвержденный рабочий заказ или risk-запись напрямую в красный риск только по совпадениям. Отдел урегулирования подтверждает повторение и имеет исключительное право снять желтую или красную запись.

---

## 14. Хранение и сроки

Минимальные сроки из Feature Specs:

* доверенный Telegram/Email действует 1 год после успешной верификации;
* неподтвержденная попытка уходит в архив через 24 часа, если не решена;
* желтая запись активна максимум 60 дней, затем становится неактивной без удаления истории;
* операционный кейс красного риска может архивироваться отдельно, но красное ограничение действует до явного снятия отделом урегулирования;
* жалобы хранятся 1 год;
* чат заказа хранится 30 дней после закрытия заказа.

Техническая реализация должна учитывать эти сроки как правила жизненного цикла данных.

---

## 15. Требования к интерфейсам

Интерфейсы должны поддерживать:

* показ замаскированных данных по умолчанию;
* раскрытие данных только через действие;
* явное отображение предупреждений риска менеджеру;
* закрытие доступа курьера после завершения доставки;
* структурированные разделы данных для администратора;
* отдельную вкладку жалоб для администратора;
* отдельный контур урегулирования;
* журнал действий.

Точные UI-компоненты раскрываются позже в Visual Rules, User Stories или Work Plans.

---

## 16. Критерии готовности

Technical Spec готов к переходу в User Stories и Work Plans, если:

* роли первого уровня определены;
* доступ строится по роли, контексту и состоянию;
* матрица доступа покрывает основные данные;
* курьер не получает клиентскую базу;
* менеджер не видит пол клиента как обычное поле заказа;
* отдел урегулирования получает данные только по ситуации;
* администратор работает внутри платформы, но не получает техническую закулису;
* раскрытие клиентских данных журналируется;
* желтый/красный список строятся по набору признаков;
* сроки хранения данных закреплены.

---

## 17. Примеры RLS-политик (SQL)

```sql
-- Принцип: default deny — все таблицы начинаются с ALTER TABLE ... ENABLE ROW LEVEL SECURITY;

-- Менеджер: доступ ко всем заказам
CREATE POLICY manager_orders_select ON orders
  FOR SELECT TO authenticated
  USING (
    (auth.jwt() -> 'app_metadata' ->> 'role') = 'MANAGER'
  );

-- Курьер: доступ только к назначенным доставкам
CREATE POLICY courier_delivery_select ON delivery_assignments
  FOR SELECT TO authenticated
  USING (
    courier_id = auth.uid()
    AND (auth.jwt() -> 'app_metadata' ->> 'role') = 'COURIER'
  );

-- Администратор: полный доступ к таблице сотрудников
CREATE POLICY admin_users_all ON user_profiles
  FOR ALL TO authenticated
  USING (
    (auth.jwt() -> 'app_metadata' ->> 'role') = 'ADMIN'
  );

-- Офицер урегулирования: доступ к resolution_cases
CREATE POLICY resolution_officer_cases ON resolution_cases
  FOR SELECT TO authenticated
  USING (
    (auth.jwt() -> 'app_metadata' ->> 'role') = 'RESOLUTION_OFFICER'
  );

-- AuditLog: append-only, INSERT для всех авторизованных, SELECT только для ADMIN
CREATE POLICY audit_log_insert ON audit_logs
  FOR INSERT TO authenticated
  WITH CHECK (true);

CREATE POLICY audit_log_select ON audit_logs
  FOR SELECT TO authenticated
  USING (
    (auth.jwt() -> 'app_metadata' ->> 'role') = 'ADMIN'
  );
```

---

## 18. Закрытые решения

1. Полный номер телефона показывается через ручное действие `Показать полный номер`, если полный номер не нужен роли автоматически. В карточке активной доставки курьера полный номер может быть показан сразу, потому что звонок является прямой задачей доставки.
2. Экспорт клиентских данных на первом этапе запрещен для всех ролей, включая администратора. Если позже экспорт понадобится, он должен быть отдельной функцией с ограничением прав, причиной экспорта и журналом.
3. Журналируется не каждое открытие карточки, а раскрытие чувствительных данных и важные действия. Это снижает шум журнала и сохраняет контроль над реальным риском.
4. На первом этапе порог желтого риска равен `4`: при `>= 4` совпавших нормализованных категориях признаков создается желтая запись. Веса признаков не используются и могут появиться только после отдельного будущего решения.
5. Журнал раскрытия клиентских данных хранится 1 год. Для событий высокого риска, связанных с желтым/красным списком, жалобами и урегулированием, запись может храниться вместе с соответствующим кейсом по его срокам хранения.
6. Красное ограничение и требование полной онлайн-оплаты не снимаются автоматически при архивировании операционного кейса; снять их может только отдел урегулирования.
7. Отдел урегулирования может снять желтую запись досрочно; иначе через 60 дней она автоматически становится неактивной, а история сохраняется.
8. **(Блок 2)** Начальные учётные записи `ADMIN` и `MANAGER` заводятся напрямую в Supabase Auth (панель проекта или разовый служебный скрипт); пароли задаёт владелец и не хранятся в репозитории. Роль передаётся через `app_metadata.role`. Guard страниц кабинетов — Next.js Middleware + проверка `employee_profiles.status = 'ACTIVE'`. Полноценное управление сотрудниками из UI — отдельный будущий блок.
9. **(Блок 2)** Точка входа персонала не размещается на публичной витрине: нет кнопки «Войти» в навбаре витрины, вместо неё — клиентская «Мои заказы». Страница входа персонала — по отдельному непубличному пути без ссылок с витрины, с `robots noindex`. Непубличный путь — это удобство против случайных заходов, а не мера безопасности; безопасность обеспечивает проверка логина/пароля и сессии.
10. **(Блок 2)** RLS восстанавливается в группе D Блока 2 по политикам §17: `default deny`; анонимный ключ витрины — только создание заказа и сообщений чата и чтение своего заказа по паре «номер заказа + код доступа» (через RPC-функцию, не открытый `SELECT`); чтение всех заказов — только `MANAGER`/`ADMIN`. До этого момента (тестовая эксплуатация) RLS может оставаться выключенной, что фиксируется в CHANGELOG как открытый долг.
11. **(Блок 2)** Для возврата клиента к заказу и чату с любого устройства заказу сопоставляется короткий код доступа (`chat_access_code`), который выдаётся только после подтверждения Telegram/Email через OTP. Хранится в виде SHA-256 хэша; в текущей упрощённой боевой схеме — колонкой `orders.chat_access_code_hash`, при вводе `client_order_access` (группа D) переносится туда. Доступ по паре «номер заказа + код» идёт через RPC `get_order_by_access` (`SECURITY DEFINER`) и заменяет повторную OTP-проверку канала на новом устройстве.
